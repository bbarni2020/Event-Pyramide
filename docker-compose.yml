services:
  app:
    build: .
    container_name: event_pyramide_app
    env_file:
      - .env
    ports:
      - "${CADDY_PORT:-5001}:5001"
    restart: unless-stopped
    volumes:
      - pgdata:/var/lib/postgresql/data
      - redisdata:/var/lib/redis

volumes:
  pgdata:
  redisdata: